# 核心
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
python-multipart>=0.0.9
pydantic>=2.6

# 图像/视频处理（可选：缺了会自动退化，但装上质量更好）
pillow>=10.0
opencv-python-headless>=4.9      # 视频抽帧 + 锐度评估
rembg>=2.0                       # segment_api.py 照片去背景
onnxruntime>=1.18                # rembg CPU inference runtime
# imageio[pyav]>=2.34            # 无 opencv 时的抽帧兜底

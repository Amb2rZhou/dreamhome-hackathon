# SAM2 抠图服务依赖(在 A10 GPU 机上装)
# torch 走默认 PyPI 即 CUDA 版(Linux x86_64,torch 2.x 默认 cu12x);
# 若被装成 CPU 版,见 README 用阿里云 pytorch 镜像重装。
ultralytics>=8.3.0
fastapi
uvicorn[standard]
pillow
numpy
python-multipart

# 资产数据审计（2026-07-22）

## 无 3D 的 57 件

- 50 件：人工不通过且原因为空，含重复、多余或误识别，不生成。
- 1 件：窗帘，备注“不完整”，按产品规则不生成。
- 1 件：被边柜替代的重复桌子，不生成。
- 2 件：桌子，备注“没补全”，补全后复审。
- 3 件：人工通过但无 3D；灯具、柜子先补全，边柜可直接生成。

实际生产候选共 5 件。

## 资产视频区间迁移

前端不再使用或记录轨迹关系。`asset_video_segments` 独立保存资产在视频中的全部
不连续出现区间。首次迁移得到 374 段，覆盖 291 件资产、9 个视频；所有带视频来源的
资产均已有区间。

同一资产可在同一视频或不同视频中拥有多段记录。例如
`ast_4fd1300eef86` 在 `vid_58a7a1504281` 中有 17 个区间，全部独立保留。

只有以下 3 件没有视频来源，因此没有视频区间：

- `ast_295e4238f0da` 原木色带抽屉边柜（rejected）
- `ast_f83ba5c15734` 实木细腿吧凳（rejected）
- `ast_d50d8f8856c6` 对照实验·绿沙发（ready，gpu-test）

人工补录只有一个时间点的资产暂存为 `t_start == t_end`，后续复验时可扩展为准确区间。
